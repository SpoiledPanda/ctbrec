# Test Build Templates

This document provides example build configurations for running tests in this project using common Java build tools.

## Maven

Add the following to your `pom.xml` to configure the Maven Surefire Plugin for running JUnit tests:

